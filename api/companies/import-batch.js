const{axios,APS,twoLegged,account,buildCompanyPayload,error}=require('../_lib');
module.exports=async(req,res)=>{try{const t=await twoLegged('account:write'),aid=account(req.body.hubId),companies=(req.body.companies||[]).filter(c=>c&&c.name).slice(0,50);if(!aid)return res.status(400).json({message:'Hub is required.'});const payload=companies.map(buildCompanyPayload);const r=await axios.post(`${APS}/hq/v1/accounts/${aid}/companies/import`,payload,{headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json'}});res.json({accountId:aid,batchNumber:req.body.batchNumber,totalBatches:req.body.totalBatches,...r.data})}catch(e){error(res,e)}};
