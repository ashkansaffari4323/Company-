const{axios,APS,twoLegged,account,buildCompanyPayload,error}=require('../../lib/_lib');module.exports=async(req,res)=>{try{const t=await twoLegged('account:write'),aid=account(req.body.hubId);if(!aid)return res.status(400).json({message:'Hub is required.'});const payload=(req.body.companies||[]).map(buildCompanyPayload).filter(Boolean).slice(0,50);if(!payload.length)return res.status(400).json({message:'No valid company names found in this batch.'});const r=await axios.post(`${APS}/hq/v1/accounts/${aid}/companies/import`,payload,{headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json'}});res.json({accountId:aid,batchNumber:req.body.batchNumber,totalBatches:req.body.totalBatches,sentItems:payload.length,...r.data})}catch(e){error(res,e)}};
