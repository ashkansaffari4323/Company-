const{APS,twoLegged,account,error}=require('../_lib');module.exports=async(req,res)=>{try{const aid=account(req.body.hubId),cid=req.body.companyId;if(!aid||!cid||!req.body.base64)return res.status(400).json({message:'hubId, companyId and image are required'});const t=await twoLegged('account:write'),buf=Buffer.from(req.body.base64,'base64'),form=new FormData();form.append('chunk',new Blob([buf],{type:req.body.mimeType||'image/png'}),req.body.fileName||'company-image.png');const r=await fetch(`${APS}/hq/v1/accounts/${aid}/companies/${cid}/image`,{method:'PATCH',headers:{Authorization:`Bearer ${t}`},body:form});const txt=await r.text();let data;try{data=JSON.parse(txt)}catch{data={raw:txt}}if(!r.ok)return res.status(r.status).json({message:data.developerMessage||data.message||'Image upload failed',details:data});res.json({company:data})}catch(e){error(res,e)}};
