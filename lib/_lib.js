const axios=require('axios');const crypto=require('crypto');const APS='https://developer.api.autodesk.com';function requireConfig(){const missing=[];for(const key of['APS_CLIENT_ID','APS_CLIENT_SECRET','APS_CALLBACK_URL','SESSION_SECRET'])if(!process.env[key])missing.push(key);if(missing.length){const e=new Error('Missing environment variables: '+missing.join(', '));e.status=400;throw e}}function basic(){return Buffer.from(`${process.env.APS_CLIENT_ID}:${process.env.APS_CLIENT_SECRET}`).toString('base64')}function key(){return crypto.createHash('sha256').update(process.env.SESSION_SECRET).digest()}function enc(obj){const iv=crypto.randomBytes(12),cipher=crypto.createCipheriv('aes-256-gcm',key(),iv),data=Buffer.concat([cipher.update(JSON.stringify(obj),'utf8'),cipher.final()]),tag=cipher.getAuthTag();return Buffer.concat([iv,tag,data]).toString('base64url')}function dec(value){try{const buffer=Buffer.from(value||'','base64url'),iv=buffer.subarray(0,12),tag=buffer.subarray(12,28),data=buffer.subarray(28),decipher=crypto.createDecipheriv('aes-256-gcm',key(),iv);decipher.setAuthTag(tag);return JSON.parse(Buffer.concat([decipher.update(data),decipher.final()]).toString())}catch{return null}}function setCookie(res,name,value){res.setHeader('Set-Cookie',`${name}=${value}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=604800`)}function clearCookie(res,name){res.setHeader('Set-Cookie',`${name}=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0`)}function getSession(req){const match=(req.headers.cookie||'').match(/(?:^|; )autodesk_session=([^;]+)/);return dec(match&&match[1])}async function refresh(session){const body=new URLSearchParams();body.set('grant_type','refresh_token');body.set('refresh_token',session.refresh_token);body.set('scope','data:read account:read account:write');const r=await axios.post(`${APS}/authentication/v2/token`,body,{headers:{Authorization:`Basic ${basic()}`,'Content-Type':'application/x-www-form-urlencoded'}});return{access_token:r.data.access_token,refresh_token:r.data.refresh_token||session.refresh_token,expires_at:Date.now()+((r.data.expires_in||3600)-60)*1000}}async function userToken(req,res){requireConfig();let session=getSession(req);if(!session){const e=new Error('Autodesk is not connected.');e.status=401;throw e}if(Date.now()>=session.expires_at){session=await refresh(session);setCookie(res,'autodesk_session',enc(session))}return session.access_token}async function twoLegged(scope='account:read account:write'){requireConfig();const body=new URLSearchParams();body.set('grant_type','client_credentials');body.set('scope',scope);const r=await axios.post(`${APS}/authentication/v2/token`,body,{headers:{Authorization:`Basic ${basic()}`,'Content-Type':'application/x-www-form-urlencoded'}});return r.data.access_token}function account(hubId){return hubId&&hubId.startsWith('b.')?hubId.slice(2):hubId||''}function clean(value){const stringValue=String(value??'').trim();if(!stringValue)return undefined;const lower=stringValue.toLowerCase();if(['0','0.0','n/a','na','null','undefined','none','-','--','#n/a'].includes(lower))return undefined;return stringValue}function cleanTrade(value){return clean(value)||process.env.DEFAULT_TRADE||'Corporate & Professional Services'}function buildCompanyPayload(company){const name=clean(company.name);if(!name)return null;return{name,trade:cleanTrade(company.trade)}}function error(res,err){res.status(err.status||err.response?.status||500).json({message:err.response?.data?.developerMessage||err.response?.data?.message||err.message||'Server error',details:err.response?.data||null})}module.exports={axios,APS,requireConfig,basic,enc,setCookie,clearCookie,getSession,userToken,twoLegged,account,clean,cleanTrade,buildCompanyPayload,error};
